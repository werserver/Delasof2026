# วิธีการรันระบบ Delasof2026 (Production Ready)

โปรเจกต์นี้ได้รับการปรับปรุงให้รองรับการเก็บข้อมูลบน Server ทั้งไฟล์ CSV และการตั้งค่า (config.json)

## ขั้นตอนการรัน

1. **ติดตั้ง Dependencies** (ทำเพียงครั้งแรก):
   ```bash
   npm install
   ```

2. **Build โปรเจกต์**:
   ```bash
   npm run build
   ```

3. **เริ่มรัน Server**:
   ```bash
   npm run server
   ```
   ระบบจะรันที่พอร์ต `3001` โดยอัตโนมัติ

## ข้อมูลที่เก็บไว้บน Server
ข้อมูลทั้งหมดจะถูกเก็บไว้ในโฟลเดอร์ `.server-data` ที่ระดับรากของโปรเจกต์:
- `.server-data/config.json`: เก็บการตั้งค่าทั้งหมดของเว็บไซต์
- `.server-data/csv/`: เก็บไฟล์ CSV ทั้งหมด (รวมถึงไฟล์หลักและไฟล์รายหมวดหมู่)

## การใช้งานหน้า Admin
1. เข้าไปที่หน้า `/admin` ของเว็บไซต์
2. ล็อกอินเข้าระบบ
3. เมื่อทำการอัปโหลด CSV หรือบันทึกการตั้งค่า ข้อมูลจะถูกส่งไปเก็บไว้ที่ Server ทันที
